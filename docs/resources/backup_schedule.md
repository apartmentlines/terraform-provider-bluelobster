# bluelobster_backup_schedule

Manages the backup schedule attached to an instance.

## Arguments

- `instance_id` required
- `frequency` required
- `hour_utc` required
- `day_of_week` optional, used by weekly schedules
- `day_of_month` optional, used by monthly schedules
